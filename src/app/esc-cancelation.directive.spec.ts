import { EscCancelationDirective } from './esc-cancelation.directive';

describe('EscCancelationDirective', () => {
  it('should create an instance', () => {
    const directive = new EscCancelationDirective();
    expect(directive).toBeTruthy();
  });
});
